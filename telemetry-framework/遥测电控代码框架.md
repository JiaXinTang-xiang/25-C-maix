[TOC]

# 遥测电控框架

## 序言

> 作为遥测电子第三届成员，在经历了25年电赛——也就是我第一年的电赛之后，我深深感受到了代码传承的重要性。
>
> 其实也并非"传承"二字所能概括。我更希望的是，往后的学弟学妹们能将更多的时间倾注在赛题的逻辑推演与算法钻研上，而不是重复搭建我们早已搭建过的轮子，不是将青春耗费在那些我们已然探索过的迷宫中。
>
> 正因如此，在诸多因缘际会之下，我开始着手制作这一版专属于遥测电子的代码框架。
>
> 愿诸位在我有限的能力之上，迸发出无限的可能。
>
> —— by 小雨 2026年7月6日

---

## 1. 简介

### 1.1 为什么要有框架

我们先看电赛现状：目前电赛难度越来越大，4天3夜的时间我们若是从0开始手搓代码，循迹、串口通信、陀螺仪、LED、Buzzer、步进电机控制、直流电机控制等等，时间根本不够用。

**没有框架的痛点：**

- 每次比赛都要重新写底层驱动，重复造轮子
- 代码风格不统一，多人协作困难
- 调试效率低，出现问题难以定位
- 模块之间耦合度高，修改一个功能影响全局
- 缺乏统一的通信协议，上下位机对接困难

**框架的目标：**

- **模块化**：各功能模块独立开发、独立测试
- **可复用**：一次编写，多次使用，积累代码资产
- **标准化**：统一的编码规范、接口定义、通信协议
- **高效率**：快速搭建项目骨架，专注于算法和业务逻辑
- **易调试**：完善的日志、状态反馈机制

### 1.2 框架适用范围

本框架适用于以 STM32F407VET6 为主控芯片的电赛项目（当然移植到其它芯片也轻轻松松），涵盖：

- 四轮/两轮小车控制
- 常用传感器，例如：灰度传感器、红外测距传感器
- 各类电机控制，例如：无刷电机、步进电机、直流电机、舵机
- 无人机/飞控系统（后续扩展）

### 1.3 硬件平台

- **主控芯片**：STM32F407VET6（Cortex-M4，168MHz）
- **开发环境**：STM32CubeMX + MDK-ARM（Keil）
- **HAL 库**：STM32F4xx_HAL_Driver

---

## 2. 框架总体架构

### 2.1 分层架构

```
┌─────────────────────────────────────────────────────────┐
│                    应用层 (Application)                    
│   Timer_task  |  Communication_task  |  IMU_tempctrl_task 
├─────────────────────────────────────────────────────────┤
│                    算法层 (Algorithm)                      
│           PID (位置式/增量式)  |  Class_PID (高级PID)        
├─────────────────────────────────────────────────────────┤
│                    设备层 (Device)                         
│   ┌──────────┬──────────┬──────────┐                     
│   │ Hardware  │   IMU    │  Motor   │                     
│   │  TB6612   │  BMI088  │ DJI/QD   │                     
│   └──────────┴──────────┴──────────┘                     
├─────────────────────────────────────────────────────────┤
│               板级支持包 (BSP / Board)                     
│   ┌─────────────────────────────────────────────┐        
│   │ 驱动: drv_spi | drv_can | drv_uart          |     
│   ├─────────────────────────────────────────────┤        
│   │ 外设: bsp_key | bsp_led | bsp_oled | bsp_buzzer│     
│   ├─────────────────────────────────────────────┤        
│   │ 工具: tool_debug | tool_delay | tool_math   │        
│   │       | tool_serialplot                     │        
│   └─────────────────────────────────────────────┘        
├─────────────────────────────────────────────────────────┤
│               硬件抽象层 (HAL)                             
│            STM32F4xx_HAL_Driver                           
├─────────────────────────────────────────────────────────┤
│                   硬件层                                   
│            STM32F407VET6 + 外设                            
└─────────────────────────────────────────────────────────┘
```

### 2.2 各层职责

| 层次 | 职责 | 说明 |
|------|------|------|
| **应用层** | 业务逻辑、任务调度 | 根据赛题需求编写，调用设备层和算法层接口，实现具体控制策略 |
| **算法层** | 控制算法 | 提供 PID 等控制算法，供设备层和应用层调用 |
| **设备层** | 外部设备驱动 | 封装具体硬件设备（电机、IMU、电机驱动板）的通信协议与控制逻辑 |
| **板级支持包 (BSP)** | 开发板外设驱动与工具 | 封装开发板上所有硬件底层驱动和通用工具函数，隔离寄存器操作 |
| **硬件抽象层 (HAL)** | ST 官方 HAL 库 | 操作寄存器，提供标准 API |
| **硬件层** | 物理芯片与外设 | STM32F407VET6 + 传感器/执行器 |

**依赖规则：上层可以调用下层，下层绝不能调用上层。** 具体来说：

- 应用层 → 可调用设备层、算法层、BSP 层
- 算法层 → 不依赖任何上层，仅提供计算能力
- 设备层 → 可调用算法层（如电机内嵌 PID）、BSP 层（如 CAN/SPI 通信）
- BSP 层 → 仅依赖 HAL 层，不依赖任何上层

### 2.3 模块总览

| 分类 | 模块 | 功能 | 文件 |
|------|------|------|------|
| **驱动** | drv_spi | SPI1 + DMA 通信 | `boards/drv_spi.c/.h` |
| **驱动** | drv_can | CAN 总线通信（收发 + 回调） | `boards/drv_can.cpp/.h` |
| **驱动** | drv_uart | UART 通信（DMA 空闲中断收发） | `boards/drv_uart.cpp/.h` |
| **外设** | bsp_key | 按键扫描（短按/长按检测） | `boards/bsp_key.c/.h` |
| **外设** | bsp_led | LED 控制 + RGB 控制 | `boards/bsp_led.c/.h` |
| **外设** | bsp_oled | 0.96寸 OLED 显示（I2C/SSD1306） | `boards/bsp_oled.c/.h` |
| **外设** | bsp_buzzer | 蜂鸣器控制 | `boards/bsp_buzzer.c/.h` |
| **工具** | tool_debug | 调试打印宏 | `boards/tool_debug.c/.h` |
| **工具** | tool_delay | 微秒/毫秒精确延时 | `boards/tool_delay.c/.h` |
| **工具** | tool_math | 数学工具（限幅、大小端、映射） | `boards/tool_math.cpp/.h` |
| **工具** | tool_serialplot | 串口绘图工具 | `boards/tool_serialplot.cpp/.h` |
| **应用** | Timer_task | 定时器任务调度 | `Appliaction/Timer_task.c/.h` |
| **应用** | communication_task | 串口通信回调处理 | `Appliaction/Communication_task.c/.h` |
| **应用** | imu_tempctrl_task | IMU 数据读取与温度控制 | `Appliaction/imu_tempctrl_task.c/.h` |
| **算法** | pid | DJI 风格 PID（位置式+增量式） | `Algorithm/pid.c/.h` |
| **算法** | alg_pid | 高级 PID（C++ 类，含前馈/死区/变积分） | `Algorithm/alg_pid.cpp/.h` |
| **硬件** | TB6612 | TB6612 直流电机驱动（PWM + 方向） | `Device/Hardware/TB6612.c/.h` |
| **IMU** | BMI088 | BMI088 六轴 IMU（SPI，含中间件层） | `Device/IMU/BMI088/*.c/.h` |
| **电机** | dvc_motor_dji | DJI 电机（GM6020/M3508，CAN + 级联PID） | `Device/Motor/Motor_DJI/*.cpp/.h` |
| **电机** | QD4310 | QD4310 电机（角度/速度/电流控制） | `Device/Motor/Motor_QD4310/*.c/.h` |

---

## 3. 板级支持包 (BSP) 详解

### 3.1 BSP 设计原则

1. **高内聚低耦合**：每个 BSP 模块只负责一个外设的驱动
2. **统一接口**：所有 BSP 模块遵循相似的命名和调用规范
3. **可配置性**：通过宏定义或结构体配置参数
4. **统一入口**：`bsp.h` 作为所有 BSP 模块的总入口

### 3.2 统一头文件 `bsp.h`

`bsp.h` 是所有模块的总入口，包含系统头文件、类型定义，以及按层分组的用户头文件：

```c
#ifndef BSP_H
#define BSP_H

/******************系统头文件***************/
#include "main.h"
#include <stdio.h>
#include <stdint.h>
#include <stdbool.h>
#include <stdarg.h>
#include <string.h>
#include <math.h>
#include "stm32f4xx_hal.h"
/*******************************************/
#define u8  unsigned char
#define u32 unsigned int
#define u16  uint16_t
#define fp32 float
#define fp64 double
#ifndef bool_t
typedef uint8_t bool_t;
#endif

/******************用户自定义头文件*********/

/* board（板卡驱动层） */
#include "drv_spi.h"
#include "drv_can.h"
#include "drv_uart.h"
#include "bsp_key.h"
#include "bsp_led.h"
#include "bsp_oled.h"
#include "bsp_buzzer.h"
#include "tool_debug.h"
#include "tool_delay.h"
#include "tool_math.h"
#include "tool_serialplot.h"

/* application（应用层） */
#include "Timer_task.h"
#include "communication_task.h"
#include "imu_tempctrl_task.h"

/* Algorithm（算法层） */
#include "pid.h"
#include "alg_pid.h"

/* Hardware（其它硬件类） */
#include "TB6612.h"

/* IMU（陀螺仪类） */
#include "BMI088driver.h"
#include "BMI088reg.h"
#include "BMI088Middleware.h"

/* Motor（电机类） */
#include "QD4310.h"
#include "dvc_motor_dji.h"

/*******************************************/
#endif
```

**说明**：各模块只需 `#include "bsp.h"` 即可使用所有功能。新增模块时，在对应层级的注释块中添加 `#include` 即可。

---

### 3.3 SPI 驱动模块 — `drv_spi`

**功能**：SPI1 总线 + DMA 收发，用于 BMI088 等高速 SPI 设备。

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `SPI1_DMA_init(tx_buf, rx_buf, num)` | 初始化 SPI1 DMA | 发送缓冲区、接收缓冲区、数据长度 | 无 |
| `SPI1_DMA_enable(tx_buf, rx_buf, ndtr)` | 启动一次 DMA 传输 | 发送缓冲区、接收缓冲区、数据长度 | 无 |

**使用示例**：

```c
#include "bsp.h"

uint8_t tx_buf[10] = {0x80, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
uint8_t rx_buf[10] = {0};

// 初始化 SPI1 DMA（只需调用一次）
SPI1_DMA_init((uint32_t)tx_buf, (uint32_t)rx_buf, 10);

// 在需要读取数据时启动 DMA 传输
SPI1_DMA_enable((uint32_t)tx_buf, (uint32_t)rx_buf, 10);

// 等 DMA 传输完成中断后，rx_buf 中即为读取的数据
```

---

### 3.4 CAN 驱动模块 — `drv_can`

**功能**：CAN 总线通信，用于与 DJI 电机驱动板等外设通信。

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `CAN_Init(hcan, Callback_Function)` | 初始化 CAN 并注册接收回调 | CAN 句柄、回调函数指针 | 无 |
| `CAN_Send_Data(hcan, ID, Data, Length)` | 发送 CAN 数据帧 | CAN 句柄、目标 ID、数据、长度 | 0=成功 |
| `TIM_1ms_CAN_PeriodElapsedCallback()` | 1ms 定时回调，发送电机控制帧 | 无 | 无 |

**关键类型**：

| 类型 | 说明 |
|------|------|
| `CAN_Call_Back` | 回调函数指针：`void (*)(Struct_CAN_Rx_Buffer *)` |
| `Struct_CAN_Rx_Buffer` | 接收缓冲区：`Header` + `Data[8]` |
| `Struct_CAN_Manage_Object` | 管理对象：CAN 句柄 + 接收缓冲区 + 回调 |

**使用示例**：

```c
#include "bsp.h"

// CAN 接收回调函数
void My_CAN_Callback(Struct_CAN_Rx_Buffer *rx_buf)
{
    // 根据 ID 分发到对应电机
    uint16_t id = rx_buf->Header.StdId;
    uint8_t *data = rx_buf->Data;
    // 处理接收数据...
}

// 初始化 CAN1 并注册回调
CAN_Init(&hcan1, My_CAN_Callback);

// 发送数据到 ID 0x200
uint8_t send_data[8] = {0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x08};
CAN_Send_Data(&hcan1, 0x200, send_data, 8);
```

---

### 3.5 UART 驱动模块 — `drv_uart`

**功能**：UART 串口通信，支持 DMA + 空闲中断接收和 DMA 发送。

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `UART_Init(huart, Callback_Function, Rx_Buffer_Length)` | 初始化 UART 并注册接收回调 | UART 句柄、回调函数、接收缓冲区长度 | 无 |
| `UART_Reinit(huart)` | 重新初始化 UART | UART 句柄 | 无 |
| `UART_Send_Data(huart, Data, Length)` | DMA 发送数据 | UART 句柄、数据指针、长度 | 0=成功 |
| `TIM_1ms_UART_PeriodElapsedCallback()` | 1ms 定时回调 | 无 | 无 |

**关键类型**：

| 类型 | 说明 |
|------|------|
| `UART_Call_Back` | 回调函数指针：`void (*)(uint8_t *Buffer, uint16_t Length)` |

**使用示例**：

```c
#include "bsp.h"

// UART 接收回调函数
void My_UART_Callback(uint8_t *Buffer, uint16_t Length)
{
    // 处理接收到的数据
    // Buffer: 接收数据缓冲区
    // Length: 实际接收到的数据长度
}

// 初始化 USART1，接收缓冲区 512 字节
UART_Init(&huart1, My_UART_Callback, 512);

// DMA 发送数据
uint8_t msg[] = "Hello World!";
UART_Send_Data(&huart1, msg, sizeof(msg));
```

---

### 3.6 按键模块 — `bsp_key`

**功能**：按键扫描，支持短按和长按检测，最多支持 2 个按键。

| 函数/宏 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `Key_GetNum()` | 获取按键事件（消费型读取） | 无 | 0=无事件, 1=KEY1短按, 2=KEY1长按, 3=KEY2短按, 4=KEY2长按 |
| `Key_Tick()` | 定时扫描（建议 10ms 调用一次） | 无 | 无 |

**按键事件码**：

| 宏 | 值 | 含义 |
|----|----|------|
| `KEY1_SHORT` | 1 | 按键1 短按 |
| `KEY1_LONG` | 2 | 按键1 长按（>1秒） |
| `KEY2_SHORT` | 3 | 按键2 短按 |
| `KEY2_LONG` | 4 | 按键2 长按 |

**使用示例**：

```c
#include "bsp.h"

// 在 10ms 定时器中断中调用
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{
    Key_Tick();  // 每 10ms 扫描一次按键
}

// 在主循环中检测按键事件
while (1)
{
    uint8_t key_event = Key_GetNum();

    if (key_event == KEY1_SHORT)
    {
        // 按键1 短按：切换红灯
        RGB_Control(1, 0, 0);
    }
    else if (key_event == KEY1_LONG)
    {
        // 按键1 长按：全灭
        RGB_Off();
    }
    else if (key_event == KEY2_SHORT)
    {
        // 按键2 短按：蜂鸣器响
        Buzzer_on();
    }
}
```

---

### 3.7 LED 控制模块 — `bsp_led`

**功能**：LED 通用控制 + RGB 三色灯控制。

#### 通用 LED 接口

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `led_on(led)` | 打开 LED（低电平有效） | `LED_Struct *led` | 无 |
| `led_off(led)` | 关闭 LED | `LED_Struct *led` | 无 |
| `led_toggle(led)` | 翻转 LED 状态 | `LED_Struct *led` | 无 |

#### RGB 三色灯接口

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `RGB_Control(r, g, b)` | 控制 RGB 灯 | r=红(1亮/0灭), g=绿, b=蓝 | 无 |
| `RGB_Off()` | 全部熄灭 | 无 | 无 |

**引脚映射宏**（修改引脚只需改这里）：

| 宏 | 映射 | 颜色 |
|----|------|------|
| `RGB_R_PORT` / `RGB_R_PIN` | LED1_GPIO_Port / LED1_Pin | 红色 |
| `RGB_G_PORT` / `RGB_G_PIN` | LED2_GPIO_Port / LED2_Pin | 绿色 |
| `RGB_B_PORT` / `RGB_B_PIN` | LED3_GPIO_Port / LED3_Pin | 蓝色 |

**使用示例**：

```c
#include "bsp.h"

// === 通用 LED（适用于任意 GPIO 引脚的 LED） ===
LED_Struct my_led = {LED1_GPIO_Port, LED1_Pin};

led_on(&my_led);       // 点亮
led_off(&my_led);      // 熄灭
led_toggle(&my_led);   // 翻转

// === RGB 三色灯 ===
RGB_Control(1, 0, 0);  // 红灯亮
RGB_Control(0, 1, 0);  // 绿灯亮
RGB_Control(0, 0, 1);  // 蓝灯亮
RGB_Control(1, 1, 0);  // 红+绿 = 黄色
RGB_Control(1, 1, 1);  // 全亮 = 白色
RGB_Off();             // 全灭
```

---

### 3.8 OLED 显示模块 — `bsp_oled`

**功能**：0.96寸 OLED 显示屏驱动（I2C 接口，SSD1306 控制器，128×64 分辨率）。

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `OLED_init()` | 初始化 OLED | 无 | 无 |
| `OLED_display_on()` | 开启显示 | 无 | 无 |
| `OLED_display_off()` | 关闭显示 | 无 | 无 |
| `OLED_operate_gram(pen)` | 操作整屏 GRAM | PEN_CLEAR/PEN_WRITE/PEN_INVERSION | 无 |
| `OLED_set_pos(x, y)` | 设置光标位置 | x: 0-127, y: 0-7 页 | 无 |
| `OLED_draw_point(x, y, pen)` | 画一个像素点 | x: 0-127, y: 0-63 | 无 |
| `OLED_draw_line(x1,y1,x2,y2,pen)` | 画直线 | 两个端点 | 无 |
| `OLED_show_char(row, col, chr)` | 显示一个字符 | 行0-4, 列0-20 | 无 |
| `OLED_show_string(row, col, chr)` | 显示字符串 | 行0-4, 列0-20 | 无 |
| `OLED_printf(row, col, fmt, ...)` | 格式化打印 | 行0-4, 列0-20, 格式字符串 | 无 |
| `OLED_refresh_gram()` | 刷新 GRAM 到屏幕 | 无 | 无 |
| `OLED_LOGO()` | 显示 RoboMaster LOGO | 无 | 无 |

**注意**：`OLED_show_char/string/printf` 操作的是 GRAM（显存），修改后需调用 `OLED_refresh_gram()` 才会更新到屏幕。`OLED_draw_point/line` 同理。

**使用示例**：

```c
#include "bsp.h"

// 初始化
OLED_init();

// 显示字符串（操作 GRAM 后需刷新）
OLED_show_string(1, 1, (uint8_t *)"Hello!");
OLED_refresh_gram();  // 刷新到屏幕

// 格式化打印
OLED_printf(2, 1, "Val:%d", 1234);
OLED_printf(3, 1, "Ang:%.2f", 3.14f);
OLED_refresh_gram();

// 画图
OLED_operate_gram(PEN_CLEAR);           // 清屏
OLED_draw_line(0, 0, 127, 63, PEN_WRITE);  // 画对角线
OLED_refresh_gram();

// 关闭显示（省电）
OLED_display_off();
```

---

### 3.9 蜂鸣器模块 — `bsp_buzzer`

**功能**：蜂鸣器开关控制（高电平有效：SET=响，RESET=静音）。

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `Buzzer_on()` | 打开蜂鸣器 | 无 | 无 |
| `Buzzer_off()` | 关闭蜂鸣器 | 无 | 无 |

**使用示例**：

```c
#include "bsp.h"

Buzzer_on();    // 蜂鸣器响
delay_ms(200);  // 响 200ms
Buzzer_off();   // 蜂鸣器停
```

---

### 3.10 调试打印模块 — `tool_debug`

**功能**：提供带文件名和行号的调试打印宏，可通过宏开关全局关闭。

| 宏 | 功能 | 说明 |
|----|------|------|
| `debug_printf(format, ...)` | 调试打印 | 输出格式：`[文件名:行号] 内容` |
| `DEBUG_LOG_ENABLE` | 日志开关 | 设为 1 启用，设为 0 则所有 `debug_printf` 编译为空 |

**使用示例**：

```c
#include "bsp.h"

int sensor_val = 42;

debug_printf("Sensor = %d\n", sensor_val);
// 输出: [main.c:5] Sensor = 42

// 比赛中关闭日志，减少 CPU 开销
// 在 tool_debug.h 中修改:
// #define DEBUG_LOG_ENABLE 0
```

---

### 3.11 延时模块 — `tool_delay`

**功能**：基于 SysTick 的微秒/毫秒精确延时，不依赖 `HAL_Delay()`。

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `delay_init()` | 初始化延时参数 | 无 | 无 |
| `delay_us(nus)` | 微秒延时 | nus: 延时微秒数 | 无 |
| `delay_ms(nms)` | 毫秒延时 | nms: 延时毫秒数 | 无 |

**使用示例**：

```c
#include "bsp.h"

delay_init();       // 系统初始化时调用一次
delay_us(100);      // 延时 100 微秒
delay_ms(500);      // 延时 500 毫秒
```

---

### 3.12 数学工具模块 — `tool_math`

**功能**：常用数学运算工具，包括限幅、大小端转换、线性映射等。

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `Math_Constrain(x, Min, Max)` | 值限幅（C++ 模板） | 值指针、最小值、最大值 | 无（原地修改） |
| `Math_Abs(x)` | 绝对值（C++ 模板） | 输入值 | 绝对值 |
| `Math_Endian_Reverse_16(Address)` | 16位大小端翻转（原地） | 地址 | 无 |
| `Math_Endian_Reverse_32(Address)` | 32位大小端翻转（原地） | 地址 | 无 |
| `Math_Sum_8(Address, Length)` | 8位累加和校验 | 数组地址、长度 | 校验和 |
| `Math_Float_To_Int(x, F1, F2, I1, I2)` | 浮点→整数线性映射 | 值、浮点范围、整数范围 | 映射后的整数 |
| `Math_Int_To_Float(x, I1, I2, F1, F2)` | 整数→浮点线性映射 | 值、整数范围、浮点范围 | 映射后的浮点数 |

**常用宏**：

| 宏 | 值 | 用途 |
|----|----|------|
| `RPM_TO_RADPS` | 2π/60 | RPM 转 rad/s |
| `DEG_TO_RAD` | π/180 | 度转弧度 |
| `CELSIUS_TO_KELVIN` | 273.15 | 摄氏度转开尔文 |

**使用示例**：

```c
#include "bsp.h"

// 限幅
float speed = 15.0f;
Math_Constrain(&speed, -10.0f, 10.0f);  // speed 被钳制为 10.0f

// 绝对值
int err = -5;
int abs_err = Math_Abs(err);  // abs_err = 5

// 大小端翻转（CAN 数据解析常用）
uint16_t raw = 0x1234;
Math_Endian_Reverse_16(&raw);  // raw 变为 0x3412

// 线性映射：将 0.0~3.3V 映射到 0~4095（ADC 值）
float voltage = 1.65f;
int32_t adc_val = Math_Float_To_Int(voltage, 0.0f, 3.3f, 0, 4095);  // ≈2048

// RPM 转 rad/s
float omega_rpm = 300.0f;
float omega_rad = omega_rpm * RPM_TO_RADPS;
```

---

### 3.13 串口绘图模块 — `tool_serialplot`

**功能**：与 SerialPlot 上位机配合，实时绘制变量波形，支持双向通信（发送数据 + 接收参数修改指令）。

| 方法 | 功能 | 说明 |
|------|------|------|
| `Init(huart, Checksum, Rx_Var_Num, Rx_Var_List, Data_Type, Frame_Header)` | 初始化 | 绑定串口、校验、接收变量列表、数据类型 |
| `Set_Data(Number, ...)` | 设置绘图数据指针 | 变参，传入各通道的 `float*` 指针 |
| `Get_Variable_Index()` | 获取接收指令的变量索引 | |
| `Get_Variable_Value()` | 获取接收指令的变量值 | |
| `TIM_1ms_Write_PeriodElapsedCallback()` | 1ms 定时发送数据 | |

**使用示例**：

```c
#include "bsp.h"

Class_Serialplot my_plot;

// 定义要绘制的变量
float plot_angle = 0.0f;
float plot_speed = 0.0f;
float plot_current = 0.0f;

// 定义可接收的变量名列表（用于上位机修参）
char *rx_var_list[] = {"kp", "ki", "kd"};

// 初始化：USART1, 无校验, 3个接收变量, FLOAT类型, 帧头0x55
my_plot.Init(&huart1,
             Serialplot_Checksum_8_DISABLE,
             3, rx_var_list,
             Serialplot_Data_Type_FLOAT,
             0x55);

// 设置 3 个绘图通道
my_plot.Set_Data(3, &plot_angle, &plot_speed, &plot_current);

// 在 1ms 定时器回调中发送数据
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{
    my_plot.TIM_1ms_Write_PeriodElapsedCallback();
}

// 主循环中更新变量值
while (1)
{
    plot_angle = ...;   // 更新角度
    plot_speed = ...;   // 更新速度
    plot_current = ...; // 更新电流
}
```

---

## 4. 应用层详解

应用层负责具体的业务逻辑和任务调度，是赛题代码的主要编写位置。

### 4.1 Timer_task — 定时器任务调度

**功能**：集中管理各模块的定时回调函数，在定时器中断中统一调用。

**使用方式**：在定时器中断回调中调用各模块的 `TIM_xxx_PeriodElapsedCallback()`：

```c
#include "bsp.h"

// 假设 TIM6 产生 1ms 中断
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{
    if (htim->Instance == TIM6)
    {
        Key_Tick();                                      // 按键扫描
        TIM_1ms_CAN_PeriodElapsedCallback();             // CAN 发送
        TIM_1ms_UART_PeriodElapsedCallback();            // UART 处理
        // 电机控制回调...
        // my_motor.TIM_Calculate_PeriodElapsedCallback();
    }
}
```

---

### 4.2 Communication_task — MaixCam 通信

**功能**：通过 USART1 向 MaixCam 发送单字节任务命令，并解析 MaixCam 返回的 8 字节测量结果。

| 函数 | 功能 | 参数 |
|------|------|------|
| `Vision_Send_Command(Command)` | 发送视觉任务并进入等待状态 | 命令 `0x01`~`0x05` |
| `Vision_Communication_Process()` | 检查 3 秒应答超时 | 无 |
| `Vision_Clear_Result()` | 释放已处理的结果或超时状态 | 无 |
| `UART_MaixCam_Call_Back(Buffer, Length)` | USART1 DMA 接收回调 | 接收缓冲区、数据长度 |

**任务命令（STM32 → MaixCam）**：

| 命令 | 任务 |
|------|------|
| `0x01` | 基础测量 |
| `0x02` | 分离正方形 |
| `0x03` | 重叠正方形 |
| `0x04` | 数字编号正方形 |
| `0x05` | 旋转目标物 |

**测量结果（MaixCam → STM32）**：

```text
AA BB DIST_H DIST_L SIZE_H SIZE_L CC DD  // 基础测量
AA BB DIST_H DIST_L SIZE_H SIZE_L EE FF  // 发挥任务
```

距离和尺寸均为大端 `uint16_t`，实际值为原始值除以 `100.0f`。

**使用示例**：

```c
UART_Init(&huart1, UART_MaixCam_Call_Back, UART_BUFFER_SIZE);

if (Vision_Data.State == Vision_State_IDLE)
{
    Vision_Send_Command(VISION_COMMAND_BASIC);
}

Vision_Communication_Process();

if (Vision_Data.State == Vision_State_RESULT_READY)
{
    /* 使用 Vision_Data.Distance_Cm 和 Vision_Data.Size_Cm */
    Vision_Clear_Result();
}
```

USART1 用于二进制协议时，不要通过该串口输出 `printf` 调试文本。

### 4.2.1 OLED 测量菜单

菜单通过 `Menu_Task_Init()` 初始化，并在主循环中周期调用 `Menu_Task_Process()`：

- KEY1 短按：向下选择，最后一项后回到第一项
- KEY2 短按：确认并执行当前测量
- 测量过程中：忽略所有按键
- 结果页：KEY1 返回菜单，KEY2 重复当前测量
- MaixCam 未标定完成：显示 `WAIT MAIXCAM`，禁止执行任务
- OLED：每 100ms 刷新一次

菜单项依次为 `BASIC`、`SEPARATED`、`OVERLAP`、`DIGIT` 和 `ROTATION`。

---

### 4.3 IMU_tempctrl_task — IMU 数据读取与温度控制

**功能**：初始化 IMU 并以 1kHz 频率读取陀螺仪、加速度计数据。

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `INS_init()` | 初始化 IMU 系统 | 无 | 无 |
| `INS_Task()` | 1kHz IMU 读取任务 | 无 | 无 |

**导出变量**：

| 变量 | 类型 | 说明 |
|------|------|------|
| `gyro[3]` | `float` | 陀螺仪原始数据 (x, y, z) rad/s |
| `accel[3]` | `float` | 加速度计原始数据 (x, y, z) m/s² |
| `temp` | `float` | IMU 温度 ℃ |
| `attitude_flag` | `uint8_t` | 姿态数据就绪标志 |

**使用示例**：

```c
#include "bsp.h"

// 初始化
INS_init();

// 主循环或定时任务中
while (1)
{
    INS_Task();  // 读取 IMU 数据

    // 使用全局变量
    float gx = gyro[0];
    float gy = gyro[1];
    float gz = gyro[2];

    // OLED 显示
    OLED_printf(1, 1, "Gx:%.1f", gx);
    OLED_printf(2, 1, "Gy:%.1f", gy);
    OLED_printf(3, 1, "Gz:%.1f", gz);
    OLED_refresh_gram();
}
```

---

## 5. 算法层详解

### 5.1 PID 控制器（DJI 风格）— `pid`

**功能**：轻量级 PID 控制器，支持位置式和增量式两种模式。

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `PID_init(pid, mode, PID[3], max_out, max_iout)` | 初始化 PID | PID结构体, 模式, Kp/Ki/Kd, 最大输出, 最大积分输出 | 无 |
| `PID_calc(pid, ref, set)` | 计算 PID 输出 | PID结构体, 反馈值, 设定值 | PID 输出值 |
| `PID_clear(pid)` | 清除 PID 状态 | PID结构体 | 无 |

**PID 模式**：

| 模式 | 说明 |
|------|------|
| `PID_POSITION` | 位置式 PID：输出 = P + I + D |
| `PID_DELTA` | 增量式 PID：输出 += ΔP + ΔI + ΔD |

**使用示例**：

```c
#include "bsp.h"

// 定义 PID 结构体
pid_type_def my_pid;

// PID 参数：Kp=1.0, Ki=0.1, Kd=0.05
fp32 pid_params[3] = {1.0f, 0.1f, 0.05f};

// 目标值和反馈值
fp32 setpoint = 100.0f;  // 设定值
fp32 feedback  = 0.0f;   // 当前反馈值
fp32 output    = 0.0f;   // PID 输出值

// 初始化 PID 控制器
// 模式：PID_POSITION (位置式)
// 最大输出：10.0
// 最大积分输出：5.0
PID_init(&my_pid, PID_POSITION, pid_params, 10.0f, 5.0f);

// 在控制循环中计算 PID 输出
output = PID_calc(&my_pid, feedback, setpoint);

// 需要重置时清除状态
PID_clear(&my_pid);
```

---

### 5.2 高级 PID 控制器 — `alg_pid`

**功能**：C++ 类式 PID，支持前馈、死区、变速积分、积分分离、微分先行等高级功能。

**核心方法**：

| 方法 | 功能 |
|------|------|
| `Init(K_P, K_I, K_D, K_F, I_Out_Max, Out_Max, D_T, ...)` | 初始化（12 个参数，有默认值） |
| `Set_Target(val)` | 设置目标值（设定值） |
| `Set_Now(val)` | 设置当前值（测量值） |
| `TIM_Calculate_PeriodElapsedCallback()` | 在定时器中调用，执行 PID 计算 |
| `Get_Out()` | 获取 PID 输出值 |

**高级参数说明**：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `K_F` | 0 | 前馈增益 |
| `I_Out_Max` | 0 | 积分限幅（0=不限幅） |
| `Out_Max` | 0 | 输出限幅（0=不限幅） |
| `D_T` | 0.001 | 采样周期（秒） |
| `Dead_Zone` | 0 | 死区范围 |
| `I_Variable_Speed_A/B` | 0 | 变速积分参数 |
| `I_Separate_Threshold` | 0 | 积分分离阈值 |
| `D_First` | DISABLE | 微分先行（ENABLE=微分在测量值上） |

**使用示例**：

```c
#include "bsp.h"

// 创建 PID 控制器
Class_PID my_pid;

// 初始化：Kp=2.0, Ki=0.5, Kd=0.1, Kf=0.01(前馈)
//          输出限幅±500, 积分限幅±200, 采样周期1ms
//          死区=2.0, 积分分离阈值=100
my_pid.Init(2.0f, 0.5f, 0.1f,    // Kp, Ki, Kd
            0.01f,                  // Kf (前馈)
            200.0f, 500.0f,         // I_Out_Max, Out_Max
            0.001f,                 // D_T = 1ms
            2.0f,                   // Dead_Zone
            0, 0,                   // 变速积分参数 A/B
            100.0f,                 // 积分分离阈值
            PID_D_First_ENABLE);    // 启用微分先行

// 在控制循环中
my_pid.Set_Target(90.0f);    // 设定目标角度 90°
my_pid.Set_Now(current_angle);  // 设定当前角度

// 在 1ms 定时器回调中计算
my_pid.TIM_Calculate_PeriodElapsedCallback();

// 获取输出
float ctrl_output = my_pid.Get_Out();
```

---

## 6. 设备层详解

### 6.1 TB6612 直流电机驱动 — `Device/Hardware/TB6612`

**功能**：TB6612 双 H 桥直流电机驱动，支持 2/4 电机控制，PWM 调速 + GPIO 方向控制。

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `TB6612_Init()` | 初始化（TIM8 PWM + GPIO） | 无 | 无 |
| `Load(moto1, moto2)` | 双电机加载 PWM | 电机1/2 的 PWM 值(-500~+500) | 无 |
| `Load4(m1, m2, m3, m4)` | 四电机加载 PWM | 四个电机的 PWM 值 | 无 |
| `Motor_Set(motor, pwm)` | 单电机控制 | MotorID_t, PWM值 | 无 |
| `Motor_Brake(motor)` | 单电机制动 | MotorID_t | 无 |
| `Motor_BrakeAll()` | 全部制动 | 无 | 无 |
| `Motor_SetLimit(max, min)` | 设置 PWM 限幅 | 最大值、最小值 | 无 |

**电机 ID**：

| 枚举值 | 电机 | TIM8 通道 |
|--------|------|-----------|
| `MOTOR_A` | 电机A | CH1 |
| `MOTOR_B` | 电机B | CH2 |
| `MOTOR_C` | 电机C | CH3 |
| `MOTOR_D` | 电机D | CH4 |

**PWM 范围**：`TB6612_PWM_MIN(-500)` ~ `TB6612_PWM_MAX(500)`，正值正转，负值反转。

**使用示例**：

```c
#include "bsp.h"

// 初始化
TB6612_Init();

// 双电机控制
Load(300, -300);     // 电机A正转(300), 电机B反转(300)

// 四电机控制（小车四轮）
Load4(300, 300, -300, -300);  // 前进

// 单电机控制
Motor_Set(MOTOR_A, 200);    // 电机A 正转 PWM=200
Motor_Set(MOTOR_B, -200);   // 电机B 反转 PWM=200

// 制动
Motor_Brake(MOTOR_A);       // 电机A 刹车
Motor_BrakeAll();            // 全部刹车

// 修改 PWM 限幅
Motor_SetLimit(400, -400);  // 限制到 ±400
```

---

### 6.2 BMI088 六轴 IMU — `Device/IMU/BMI088`

**功能**：BMI088 六轴惯性测量单元（3轴加速度计 + 3轴陀螺仪），SPI 接口通信。

#### BMI088driver（主驱动）

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `BMI088_init()` | 初始化 IMU（含自检） | 无 | 错误码（0=成功） |
| `BMI088_read(gyro, accel, temperate)` | 读取所有数据 | 陀螺仪[3]、加速度[3]、温度 | 无 |
| `get_BMI088_accel(accel)` | 获取缩放后加速度 | 加速度[3] | 无 |
| `get_BMI088_temperate()` | 获取温度 | 无 | 温度值 ℃ |

#### BMI088Middleware（移植层）

| 函数 | 功能 |
|------|------|
| `BMI088_delay_ms(ms)` | 毫秒延时 |
| `BMI088_delay_us(us)` | 微秒延时 |
| `BMI088_ACCEL_NS_L()` / `H()` | 加速度计片选拉低/拉高 |
| `BMI088_GYRO_NS_L()` / `H()` | 陀螺仪片选拉低/拉高 |

**使用示例**：

```c
#include "bsp.h"

bmi088_real_data_t imu_data;

// 初始化（含自检，失败会返回非零）
delay_init();
while (BMI088_init())
{
    delay_ms(100);  // 自检失败则重试
}

// 读取数据
while (1)
{
    BMI088_read(imu_data.gyro, imu_data.accel, &imu_data.temp);

    // imu_data.gyro[0/1/2] = x/y/z 轴角速度 (rad/s)
    // imu_data.accel[0/1/2] = x/y/z 轴加速度 (m/s²)
    // imu_data.temp = 温度 (℃)

    OLED_printf(1, 1, "Gx:%.1f", imu_data.gyro[0]);
    OLED_printf(2, 1, "Ax:%.1f", imu_data.accel[0]);
    OLED_printf(3, 1, "T:%.1f", imu_data.temp);
    OLED_refresh_gram();

    delay_ms(10);
}
```

---

### 6.3 DJI 电机驱动 — `Device/Motor/Motor_DJI/dvc_motor_dji`

**功能**：DJI 系列电机（GM6020 云台电机 / M3508 底盘电机），CAN 总线通信，内置级联 PID 控制。

**控制模式**：

| 模式 | 说明 |
|------|------|
| `VOLTAGE` | 电压控制（开环） |
| `CURRENT` | 电流控制（单环） |
| `TORQUE` | 扭矩控制 |
| `OMEGA` | 角速度控制（电流内环 + 角速度外环） |
| `ANGLE` | 角度控制（角度外环 + 角速度中环 + 电流内环） |

**CAN ID 映射**：

| 枚举值 | CAN 接收 ID | 电机 |
|--------|-------------|------|
| `Motor_DJI_ID_0x201` | 0x201 | 电机1 |
| `Motor_DJI_ID_0x202` | 0x202 | 电机2 |
| ... | ... | ... |
| `Motor_DJI_ID_0x20B` | 0x20B | 电机11 |

**使用示例**：

```c
#include "bsp.h"

Class_Motor_DJI_GM6020 my_motor;

// 初始化：CAN1, ID=0x201, 角度控制模式
my_motor.Init(&hcan1, Motor_DJI_ID_0x201, ANGLE);

// 配置角度环 PID 参数
my_motor.PID_Angle.Set_K_P(5.0f);
my_motor.PID_Angle.Set_K_I(0.0f);
my_motor.PID_Angle.Set_K_D(0.1f);

// 配置角速度环 PID 参数
my_motor.PID_Omega.Set_K_P(10.0f);
my_motor.PID_Omega.Set_K_I(0.5f);
my_motor.PID_Omega.Set_K_D(0.0f);

// 设置目标角度（弧度）
my_motor.Set_Target_Angle(1.57f);  // 目标 90°

// 在定时器中断中调用
// 1ms 定时器：PID 计算
my_motor.TIM_Calculate_PeriodElapsedCallback();

// 100ms 定时器：存活检测
my_motor.TIM_100ms_Alive_PeriodElapsedCallback();

// 读取当前状态
float now_angle = my_motor.Get_Now_Angle();
float now_omega = my_motor.Get_Now_Omega();
float now_temp  = my_motor.Get_Now_Temperature();

// 切换控制模式
my_motor.Set_Control_Method(OMEGA);
my_motor.Set_Target_Omega(3.14f);  // 目标角速度 π rad/s
```

---

### 6.4 QD4310 电机驱动 — `Device/Motor/Motor_QD4310/QD4310`

**功能**：QD4310 电机驱动，支持角度、速度、电流控制。

| 函数 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `QD4310_Init(motor, hcan, id)` | 初始化电机 | 电机结构体、CAN句柄、ID(1-8) | 无 |
| `QD4310_Enable(motor)` | 使能电机 | 电机结构体 | 无 |
| `QD4310_Disable(motor)` | 失能电机 | 电机结构体 | 无 |
| `QD4310_SetAngle(motor, angle)` | 设置角度 | 电机结构体、角度[0, 2π] | 无 |
| `QD4310_SetSpeed(motor, speed)` | 设置速度 | 电机结构体、速度[-1000, 1000] rpm | 无 |
| `QD4310_SetCurrent(motor, current)` | 设置电流 | 电机结构体、电流[-10, 10] A | 无 |
| `QD4310_Update(motor, feedback)` | 更新反馈数据 | 电机结构体、8字节反馈帧 | 无 |

**使用示例**：

```c
#include "bsp.h"

QD4310_t my_motor;

// 初始化：CAN1, ID=1
QD4310_Init(&my_motor, &hcan1, 1);

// 使能电机
QD4310_Enable(&my_motor);

// 速度控制
QD4310_SetSpeed(&my_motor, 500.0f);    // 500 rpm

// 角度控制
QD4310_SetAngle(&my_motor, 1.57f);     // 转到 90° (π/2)

// 电流控制
QD4310_SetCurrent(&my_motor, 2.0f);    // 2A 电流

// 失能
QD4310_Disable(&my_motor);

// 读取反馈（在 CAN 接收回调中更新）
// my_motor.speed   → 当前速度 (rpm)
// my_motor.angle   → 当前角度 (rad)
// my_motor.current → 当前电流 (A)
```

---

## 7. 项目文件结构

```
Telemetry framework/
├── .mxproject                          # CubeMX 项目配置
├── Telemetry framework.ioc             # CubeMX 图形化配置
├── 遥测电控代码框架.md                   # 本文档
│
├── Core/                               # CubeMX 生成的代码
│   ├── Inc/
│   │   ├── main.h
│   │   ├── gpio.h
│   │   ├── usart.h
│   │   ├── spi.h
│   │   ├── dma.h
│   │   ├── stm32f4xx_hal_conf.h
│   │   └── stm32f4xx_it.h
│   └── Src/
│       ├── main.c                      # 主程序入口
│       ├── gpio.c
│       ├── usart.c
│       ├── spi.c
│       ├── dma.c
│       ├── stm32f4xx_hal_msp.c
│       ├── stm32f4xx_it.c              # 中断服务函数
│       └── system_stm32f4xx.c
│
├── boards/                             # BSP 板级支持包
│   ├── bsp.h                           # BSP 统一头文件（总入口）
│   │
│   ├── drv_spi.c/.h                    # SPI 驱动
│   ├── drv_can.cpp/.h                  # CAN 驱动
│   ├── drv_uart.cpp/.h                 # UART 驱动
│   │
│   ├── bsp_key.c/.h                    # 按键
│   ├── bsp_led.c/.h                    # LED + RGB
│   ├── bsp_oled.c/.h                   # OLED 显示
│   ├── bsp_buzzer.c/.h                 # 蜂鸣器
│   │
│   ├── tool_debug.c/.h                 # 调试打印
│   ├── tool_delay.c/.h                 # 延时
│   ├── tool_math.cpp/.h                # 数学工具
│   ├── tool_serialplot.cpp/.h          # 串口绘图
│   │
│   └── oledfont.h                      # OLED 字库
│
├── Appliaction/                        # 应用层
│   ├── Timer_task.c/.h                 # 定时器任务
│   ├── Communication_task.c/.h         # 通信任务
│   └── imu_tempctrl_task.c/.h          # IMU 温控任务
│
├── Algorithm/                          # 算法层
│   ├── pid.c/.h                        # DJI 风格 PID
│   └── alg_pid.cpp/.h                  # 高级 PID (C++)
│
├── Device/                             # 设备层
│   ├── Hardware/
│   │   └── TB6612.c/.h                 # TB6612 电机驱动
│   ├── IMU/
│   │   └── BMI088/
│   │       ├── BMI088driver.c/.h       # BMI088 主驱动
│   │       ├── BMI088Middleware.c/.h   # BMI088 移植层
│   │       └── BMI088reg.h             # BMI088 寄存器定义
│   └── Motor/
│       ├── Motor_DJI/
│       │   └── dvc_motor_dji.cpp/.h    # DJI 电机
│       └── Motor_QD4310/
│           └── QD4310.c/.h             # QD4310 电机
│
├── Drivers/                            # ST 官方驱动
│   ├── CMSIS/
│   └── STM32F4xx_HAL_Driver/
│
├── Middlewares/                        # 中间件
│
└── MDK-ARM/                            # Keil 工程文件
    ├── Telemetry framework.uvprojx
    ├── Telemetry framework.uvoptx
    └── ...
```

---

## 8. 快速上手指南

### 8.1 新建项目流程

1. **使用 CubeMX 配置**
   - 选择芯片：STM32F407VET6
   - 配置时钟：HSE 外部晶振，PLL 倍频至 168MHz
   - 配置外设：USART / SPI / CAN / GPIO / TIM / DMA 等
   - 生成 MDK-ARM 工程

2. **添加框架代码**
   - 将 `boards/`、`Appliaction/`、`Algorithm/`、`Device/` 文件夹复制到工程目录
   - 在 Keil 中添加对应源文件和头文件路径
   - 在 `main.c` 中 `#include "bsp.h"`

3. **编写应用代码**

```c
/* main.c */
#include "bsp.h"

int main(void)
{
    HAL_Init();
    SystemClock_Config();
    MX_GPIO_Init();
    MX_DMA_Init();
    MX_USART1_UART_Init();
    MX_SPI1_Init();

    // BSP 初始化
    delay_init();
    OLED_init();
    TB6612_Init();

    // IMU 初始化
    while (BMI088_init())
    {
        delay_ms(100);
    }

    // UART 初始化（注册回调）
    UART_Init(&huart1, UART_MaixCam_Call_Back, UART_BUFFER_SIZE);

    OLED_show_string(1, 1, (uint8_t *)"System Ready!");
    OLED_refresh_gram();

    while (1)
    {
        // 主循环业务逻辑
    }
}
```

### 8.2 添加新模块的规范

1. 确定模块属于哪一层：`boards/`（BSP）、`Appliaction/`（应用）、`Algorithm/`（算法）、`Device/`（设备）
2. 在对应目录下创建 `.c/.h` 文件
3. 头文件使用 `#ifndef` 防止重复包含
4. 在 `bsp.h` 中对应层级的注释块里添加 `#include`
5. 函数命名规范：`模块名_功能名()`（如 `LED_On()`、`Buzzer_On()`）

---

## 9. 编码规范

### 9.1 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 宏定义 | 全大写 + 下划线 | `#define PWM_MAX 500` |
| 全局变量 | 小驼峰 | `uint8_t rxData[10]` |
| 函数 | 模块名_功能名 | `void Buzzer_On(void)` |
| 类型定义 | 小写 + `_t` / `_typedef` | `typedef uint8_t bool_t` |
| C++ 类 | `Class_` 前缀 + 大驼峰 | `Class_Motor_DJI_GM6020` |
| 文件命名 | 小写 + 下划线 | `bsp_led.c` |

### 9.2 注释规范

```c
/**
 * @brief 函数功能描述
 * @param 参数名 参数说明
 * @retval 返回值说明
 */
```

### 9.3 文件头注释

```c
/**
  ******************************************************************************
  * @file           : filename.c
  * @brief          : 文件功能描述
  ******************************************************************************
  */
```

### 9.4 代码风格建议

- **缩进**：使用 4 个空格缩进
- **大括号**：换行风格（Allman 风格）
- **行宽**：每行不超过 80 个字符
- **空行**：函数之间保留一个空行，逻辑块之间适当空行
- **编码**：所有源文件统一使用 **UTF-8** 编码

---

## 10. 调试与测试

### 10.1 调试方法

| 方法 | 说明 | 适用场景 |
|------|------|----------|
| **串口打印** | `debug_printf()` 输出调试信息 | 通用调试，变量监控 |
| **OLED 显示** | `OLED_printf()` 实时显示关键变量 | 现场调试，无需电脑 |
| **SerialPlot** | `Class_Serialplot` 绘制变量波形 | 控制算法调参，动态分析 |
| **LED 指示** | `RGB_Control()` 不同颜色表示不同状态 | 快速状态判断 |
| **蜂鸣器** | `Buzzer_on()`/`Buzzer_off()` 声音提示 | 错误报警，状态提示 |

### 10.2 常见问题排查

| 问题 | 可能原因 | 解决方法 |
|------|----------|----------|
| OLED 不显示 | I2C 接线错误 / 地址不对 | 检查 SCL/SDA 引脚，确认 0x78 地址 |
| 串口无数据 | 波特率不匹配 / 引脚错误 | 核对波特率，检查 TX/RX 连接 |
| 电机不转 | CAN 通信失败 / PID 参数错误 | 检查 CAN 滤波器配置，调整 PID 参数 |
| IMU 数据异常 | SPI 通信失败 / 自检未通过 | 检查 SPI 接线，查看 `BMI088_init()` 返回值 |
| TB6612 电机不转 | PWM 未使能 / 方向引脚错误 | 检查 TIM8 配置，确认 IN1/IN2 引脚 |
| CAN 通信失败 | 波特率不一致 / 滤波器配置 | 核对所有节点波特率，检查滤波器 |

---

## 11. 扩展与展望

### 11.1 计划添加的模块

- [ ] **遥控器解码**：SBUS / DBUS 协议完整实现
- [ ] **编码器读取**：电机速度 / 位置测量
- [ ] **Flash 存储**：参数掉电保存
- [ ] **FreeRTOS**：实时操作系统支持
- [ ] **超声波 / 红外**：避障传感器

### 11.2 框架演进路线

```
v1.0 - 基础 BSP 模块 + BMI088 + UART 通信（当前）
v1.1 - 完善电机驱动 + PID 调参工具
v1.2 - 遥控器支持 + 通信协议层
v2.0 - 引入 FreeRTOS + 任务调度
v2.1 - 完善上位机通信协议 + SerialPlot 联调
v3.0 - 通用化、跨平台支持
```

---

## 12. 附录

### 12.1 STM32F407VET6 资源概览

| 资源 | 数量 / 规格 |
|------|-------------|
| 内核 | Cortex-M4 @ 168MHz |
| Flash | 512KB |
| SRAM | 192KB |
| USART / UART | 6 路 |
| CAN | 2 路 |
| SPI | 3 路 |
| I2C | 3 路 |
| 定时器 | 14 个 |
| GPIO | 82 个 |

### 12.2 常用宏定义速查

```c
// GPIO 操作
#define LED_ON()      HAL_GPIO_WritePin(LED_GPIO_Port, LED_Pin, GPIO_PIN_RESET)
#define LED_OFF()     HAL_GPIO_WritePin(LED_GPIO_Port, LED_Pin, GPIO_PIN_SET)
#define LED_TOGGLE()  HAL_GPIO_TogglePin(LED_GPIO_Port, LED_Pin)

// 限幅宏
#define CLAMP(x, min, max)  ((x) < (min) ? (min) : ((x) > (max) ? (max) : (x)))

// 数组大小
#define ARRAY_SIZE(arr)     (sizeof(arr) / sizeof((arr)[0]))

// 位操作
#define BIT_SET(reg, bit)   ((reg) |= (1 << (bit)))
#define BIT_CLR(reg, bit)   ((reg) &= ~(1 << (bit)))
#define BIT_GET(reg, bit)   (((reg) >> (bit)) & 0x01)
#define BIT_TOGGLE(reg, bit) ((reg) ^= (1 << (bit)))
```

### 12.3 常用 STM32 HAL 函数速查

| 函数 | 功能 |
|------|------|
| `HAL_GPIO_WritePin()` | 设置 GPIO 输出电平 |
| `HAL_GPIO_TogglePin()` | 翻转 GPIO 输出电平 |
| `HAL_GPIO_ReadPin()` | 读取 GPIO 输入电平 |
| `HAL_UART_Transmit()` | 串口阻塞发送 |
| `HAL_UART_Transmit_DMA()` | 串口 DMA 发送 |
| `HAL_UARTEx_ReceiveToIdle_DMA()` | 串口 DMA + 空闲中断接收 |
| `HAL_CAN_Start()` | 启动 CAN 通信 |
| `HAL_CAN_AddTxMessage()` | CAN 发送消息 |
| `HAL_SPI_TransmitReceive()` | SPI 阻塞收发 |
| `HAL_TIM_PWM_Start()` | 启动定时器 PWM 输出 |

---

> **文档版本**：v2.0
> **最后更新**：2026 年 7 月
